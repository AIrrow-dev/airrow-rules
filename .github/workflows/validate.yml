name: Validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate stable bundle
        run: python scripts/build_bundle.py --check --output bundle/stable.bundle.json

      - name: Run unit tests
        run: python -m unittest discover -s tests
